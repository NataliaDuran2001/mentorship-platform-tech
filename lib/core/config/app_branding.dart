// Core layer (Config): Brand identity. The only place where the product name
// lives (mockups: "Obsidian Mentor"; package: aspire_app), so no other file
// should hardcode it.

class AppBranding {
  static const String name = 'Kora';
}
